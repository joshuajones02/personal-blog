@model Piranha.Extend.Blocks.ImageBlock
@{
    var alt = Model?.Body?.Media?.AltText ?? "";
}

<figure>
    <img src="@Url.Content(WebApp.Media.ResizeImage(Model, 1280))"
         alt="@alt"
         loading="lazy"
         decoding="async">
</figure>
