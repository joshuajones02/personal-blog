@model Piranha.Extend.Blocks.QuoteBlock

<blockquote class="block-quote">
    <p>@Model.Body</p>
    @if (!string.IsNullOrWhiteSpace(Model.Author))
    {
        <footer>— <cite>@Model.Author</cite></footer>
    }
</blockquote>
