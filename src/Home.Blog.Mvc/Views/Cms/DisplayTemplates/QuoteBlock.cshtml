@model Piranha.Extend.Blocks.QuoteBlock

<figure class="quote">
    <blockquote>
        <p>@Model.Body</p>
    </blockquote>
    @if (!string.IsNullOrWhiteSpace(Model.Author))
    {
        <figcaption><cite>@Model.Author</cite></figcaption>
    }
</figure>
