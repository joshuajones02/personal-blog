@model Piranha.Extend.Blocks.PageBlock

@if (Model.Body.Page != null)
{
    <article class="block-ref">
        @if (Model.Body.Page.PrimaryImage.HasValue)
        {
            <div class="block-ref__media">
                <img src="@Url.Content(WebApp.Media.ResizeImage(Model.Body.Page.PrimaryImage, 640, 420))" alt="" loading="lazy">
            </div>
        }
        <div class="block-ref__body">
            <h3 class="block-ref__title"><a href="@WebApp.Url(Model)">@Model.Body.Page.Title</a></h3>
            <p>@Html.Raw(Model.Body.Page.Excerpt)</p>
        </div>
    </article>
}