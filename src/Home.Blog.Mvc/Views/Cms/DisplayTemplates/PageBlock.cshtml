@model Piranha.Extend.Blocks.PageBlock

@if (Model.Body.Page != null)
{
    <div class="card h-100">
        @if (Model.Body.Page.PrimaryImage.HasValue)
        {
            <div class="card-img-wrapper">
                <a href="@WebApp.Url(Model)">
                    <img class="card-img-top" src="@Url.Content(WebApp.Media.ResizeImage(Model.Body.Page.PrimaryImage, 540, 300))" alt="@Model.Body.Page.Title">
                </a>
            </div>
        }
        <div class="card-body d-flex flex-column">
            <h5 class="card-title">@Model.Body.Page.Title</h5>
            <p class="card-text flex-grow-1">@Html.Raw(Model.Body.Page.Excerpt)</p>
            <a href="@WebApp.Url(Model)" class="read-more mt-auto">Learn More</a>
        </div>
    </div>
}
